# 📅 2026년 5월 11일 (월) 개발 일지 및 작업 요약

## 🚀 달성 목표: 기초 시스템 구축 및 코어 메카닉 연동 완료

### 🛠️ Phase 1. 기초 셋업 (아키텍처 및 렌더링)

- **프로젝트 초기화:** 작업 문서(`checklist.md`) 생성 및 Paper2D / PaperZD 등 2D 필수 플러그인 활성화 완료.
- **상호작용 기반 마련:** 부모 클래스 `ABlock` C++ 생성 및 다형성을 활용한 `OnInteracted` 가상 함수(Virtual Function) 뼈대 구축.
- **메모리 최적화 맵 매니저:** `AMapManager` C++ 생성, `TArray` 기반의 블록 풀링 선언 및 `Reserve`를 통한 선제적 메모리 할당 적용.
- **그리드 스폰 로직:** 초기 라인 단위 블록 생성 C++ 로직 구현 및 에디터 월드 아웃라이너에서 정상 스폰(배치) 검증 완료.
- **2D 에셋 파이프라인 구축:** 스프라이트 시트 슬라이싱 및 기본 플립북(Flipbook) 에셋 생성 테스트 완료.
- **플레이어 셋업:** `ADrillerCharacter` 베이스 구축 (직교 카메라 세팅, 캡슐 콜리전 조율, 시작 Y축 좌표 고정 및 물리 설정).

### 🏃‍♂️ Phase 2. 코어 메카닉 기초 (입력 및 애니메이션)

- **Enhanced Input 시스템 도입:**
    - `IA_Move`, `IA_Dig`, `IMC_Default` 에셋 생성 및 모디파이어(Negate) 설정 완료.
    - `SetupPlayerInputComponent`에서 C++ 함수(`Move`, `Dig`)와 액션 바인딩 완료.
    - 아케이드 감성을 살리기 위한 이동 물리(`CharacterMovement`) 마찰력 및 가속도 극한 튜닝.
- **PaperZD 애니메이션 상태 머신 구축:**
    - 이동 방향(`Velocity`)에 따른 스프라이트 좌/우 반전(Yaw 180도 회전) C++ 로직 구현.
    - `ABP_Driller` (AnimGraph) 생성 및 속도 기반의 Idle <-> Run 상태 전이(State Machine) 완벽 연동.
    - Tick 비활성화 및 라이브 코딩 캐싱으로 인한 디버그 멈춤 이슈 완벽 트러블슈팅.
- **채굴(Dig) 레이캐스트 뼈대:**
    - 스페이스바(`IA_Dig`) 입력 시 C++ `Dig()` 함수 호출 및 로그(`Dig Action Triggered!`) 검증 완료.
    - 캐릭터가 바라보는 방향을 향한 Line Trace(레이캐스트) 탐지 로직 뼈대 및 `ABlock`의 `OnInteracted()` 호출 준비 완료.