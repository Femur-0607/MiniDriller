# checklist

# 오늘의 목표 (Phase 1 기초 셋업)

- [ ]  1단계 작업 문서(checklist.md) 생성 및 필수 플러그인 활성화 확인
- [ ]  2단계 부모 클래스 `ABlock` 생성 및 다형성을 위한 가상 함수 `OnInteracted` 뼈대 작성
- [ ]  3단계 `AMapManager` 생성 및 `TArray` 기반 블록 풀 선언 (`Reserve` 메모리 할당 포함)
- [ ]  4단계 초기 라인 단위 블록 생성 로직 구현 및 에디터 월드 아웃라이너에서 올바른 스폰 확인