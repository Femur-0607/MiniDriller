# checklist

## 2026년 5월 11일 의 목표

### (Phase 1 기초 셋업)

- [x]  1단계 작업 문서(checklist.md) 생성 및 필수 플러그인 활성화 확인
- [x]  2단계 부모 클래스 `ABlock` 생성 및 다형성을 위한 가상 함수 `OnInteracted` 뼈대 작성
- [x]  3단계 `AMapManager` 생성 및 `TArray` 기반 블록 풀 선언 (`Reserve` 메모리 할당 포함)
- [x]  4단계 초기 라인 단위 블록 생성 로직 구현 및 에디터 월드 아웃라이너에서 올바른 스폰 확인
- [x]  기본 스프라이트 자르기 및 플립북 제작 테스트
- [x]  `ADrillerCharacter` 캐릭터 기본 세팅  (카메라 뷰, 블록 충돌체, 시작 좌표 고정)

### (Phase 2. 코어 메카닉 기초)

- [x]  **1단계: Enhanced Input (IMC, IA) 세팅 및 좌우 이동 구현**
    - 에디터에서 `IA_Move`, `IA_Dig` 액션과 `IMC_Default` 매핑 컨텍스트 에셋 생성하기.
    - `ADrillerCharacter.cpp`에서 `SetupPlayerInputComponent` 함수를 통해 키보드(A, D 또는 화살표) 입력을 `Move` 함수에 바인딩하고 캐릭터 좌우로 움직이기.
- [x]  **2단계: 캐릭터 방향 전환 및 기본 애니메이션(Flipbook) 연동**
    - 이동 방향(Velocity)에 따라 캐릭터 스프라이트가 좌/우를 바라보도록 회전(또는 Scale X 반전) 처리하기.
    - 대기 상태(Idle)와 달리기(Run) 플립북 에셋을 만들고, 코드에서 상태에 따라 플립북 전환해주기.
- [x]  **3단계: 드릴 채굴(Dig) 뼈대 로직 작성**
    - `IA_Dig` (예: 스페이스바) 입력 시 `Dig()` 함수 호출하기.
    - Line Trace(레이캐스트)를 사용하여 캐릭터가 바라보는 방향(아래 혹은 옆)에 있는 `ABlock` 액터를 감지하고, 해당 블록의 `OnInteracted()` 함수 호출하기.

## 2026년 5월 12일 의 목표

**(Phase 3. 채굴 및 블록 파괴 시스템)**

- [ ]  **1단계: 채굴 레이캐스트(Line Trace) 구현**
    - `ADrillerCharacter::Dig()` 함수 내부에 Line Trace 로직 작성.
    - 캐릭터가 바라보는 방향(혹은 아래)으로 레이캐스트를 쏴서 `ABlock` 감지하기.
- [ ]  **2단계: 블록 파괴 (Object Pooling 반환)**
    - 레이캐스트에 맞은 `ABlock`의 `OnInteracted()` 함수 호출.
    - 블록 파괴 시 파티클/사운드 재생 및 `AMapManager`의 오브젝트 풀로 반환(비활성화) 처리.
- [ ]  **3단계: 채굴 애니메이션(Dig) 연동**
    - PaperZD 애니메이션 블루프린트에 `Dig` 상태 추가 및 로직 연동 (Any State -> Dig 처리).
    - 애니메이션 재생 중 이동 입력 제한(선택 사항) 로직 고민해보기.

**(Phase 4. 중력 및 블록 낙하 시스템 - 시간 여유 시)**

- [ ]  **1단계: 빈 공간 감지**
    - 아래쪽 블록이 파괴되었을 때, 위에 있는 블록들이 빈 공간을 인식하는 로직 구상 (Raycast 또는 Grid 배열 갱신).
- [ ]  **2단계: 낙하 처리**
    - 허공에 뜬 블록들이 아래로 떨어지는 물리 혹은 보간(Lerp) 이동 로직 적용.